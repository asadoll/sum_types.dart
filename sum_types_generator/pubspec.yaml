name: sum_types_generator
description: sum_types and sum_types_generator packages together define a code generator enabling sum-types in Dart.
version: 0.3.1
homepage: https://github.com/werediver/sum-types-dart

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  analyzer: ^4.0.0
  build: ^2.0.1
  meta: ^1.1.0
  source_gen: ^1.0.0
  sum_types:
    git:
      url: https://github.com/asadoll/sum_types.dart.git
      ref: master # branch name
      path: sum_types

dev_dependencies:
  dependency_validator:
